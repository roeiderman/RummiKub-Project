{
  "name": "rummikub-project",
  "version": "1.0.0",
  "scripts": {
    "iba": "npm install --prefix Backend && npm install --prefix mobile-app && pip3 install --break-system-packages -r model/requirements.txt && pip3 install --break-system-packages -r solver_engine/requirements.txt"
  }
}
